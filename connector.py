Use case:
use built in Kafka connectors to  monitor a file(access_log.txt.txt) and publish new lines on that file to a 
given Kafka topic that then get written out to some other file(logout.txt) somewhere else.

